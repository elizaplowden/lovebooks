<h2>Choose the books you want to add to your list!</h2>

<div class="holder">
  <% @books.each do |book| %>
    <%= tag.div class: "bi-book-card", id: dom_id(book) do %>
      <div class="book-image">
        <%= image_tag book.image_url %>
      </div>
        <div class="buttons-holder">
          <button type="button" class="btn btn-warning but-button" id="<%= book.id %>">Not interested</button>
          <%= form_with(model: BookList.new, local: false) do |f| %>
            <%= f.hidden_field :book_id, value:book.id %>
            <%= f.hidden_field :list_id, value: current_user.lists.first.id %>
            <%= f.submit "Add book", class: 'btn btn-primary but-button' %>
          <% end %>
        </div>
    <% end %>
  <% end %>
</div>
